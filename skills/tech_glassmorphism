# 🌫️ 毛玻璃科技风排版技能 (glassmorphism)

## 适用场景
技术教程、开发工具介绍、偏极客的科技分享，干净专业又不失现代感。

## 设计规范
- **页面背景色**：`#f4f7fc`（浅蓝灰），干净透气
- **主标题 h1**：字号 `26px`，颜色 `#0f172a`，加粗 `700`，居中
- **副标题**：字号 `14px`，颜色 `#64748b`，居中，字母间距 `1px`
- **卡片**：背景 `#ffffff`，圆角 `16px`，阴影 `0 4px 12px rgba(0,0,0,0.04)`，卡片间距 `20-28px`
- **大标题 h2**：字号 `20px`，颜色 `#1e293b`，左侧 `4px solid #3b82f6`，加粗 `600`
- **小标题 h3**：字号 `17px`，颜色 `#334155`，加粗 `600`
- **正文 p**：字号 `14-15px`，颜色 `#475569`，行高 `1.7`
- **行内代码 code**：背景 `#e9eef2`，文字色 `#c62828`，圆角 `4px`，内边距 `2px 8px`
- **多行代码块 pre**：背景 `#1a202c`，文字色 `#e2e8f0`，圆角 `10px`，内边距 `16px`
- **子区块/说明框**：背景 `#f8fafd`，边框 `1px solid #e9eff6`，圆角 `12px`，内边距 `16px`
- **引用块**：背景 `#f0f4ff`，左侧 `4px solid #3b82f6`，圆角 `12px`
- **图片占位**：虚线 `1px dashed #cbd5e1`，背景 `#f8fafc`，圆角 `12px`，居中显示 🖼️
- **列表 ul/ol**：颜色 `#475569`，行高 `1.9`
- **页眉**：居中，`🔷 栏目名`，颜色 `#64748b`
- **页脚**：顶部 `1px solid #e9eff6`，颜色 `#64748b`，字号 `13px`，居中
- **字体**：`'Alibaba PuHuiTi', 'Source Han Sans SC', 'PingFang SC', 'Microsoft YaHei', 'Helvetica Neue', sans-serif`
- **容器**：`max-width: 680px; margin: 0 auto; padding: 24px 16px 10px`

## HTML 结构要求
- 所有样式内联，不使用 class
- 主标题和副标题居中
- 每个大章节用一个白色卡片包裹
- 卡片之间通过间距和淡阴影区分
- 大标题 h2 用蓝色左边框，小标题 h3 无边框
- 代码块深色底 + 浅色字，对比清晰
- 不使用半透明、模糊等效果
- 页脚必须有

## 生成指令
1. 生成页眉（居中，🔷 + 栏目名 + 日期）
2. 主标题居中，副标题居中
3. 按文章内容拆分章节，每个章节用白卡包裹
4. 大标题 h2 带蓝色左边框，小标题 h3 不加边框
5. 代码块深色底 #1a202c，行内代码浅灰底
6. 子区块用浅灰蓝底 + 细边框
7. 引用块用淡蓝底 + 蓝色左边框
8. 页脚
9. 输出 HTML 片段（不含 <!DOCTYPE>）
